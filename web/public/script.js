// The real flag is encoded, but can you decode it? 😏
let encodedFlag = "Q1RGezEyMzRfczRyXzFtcDNyfQ=="; // Base64 Encoded

document.addEventListener("DOMContentLoaded", () => {
    console.log("Hmm... Maybe check the console? 😉");
    console.log("Decode this: " + encodedFlag);
});
